import type { Styles } from '@/types/index.d.ts';

/**
 * Styles.
 *
 * @since 1.0.0
 */
export const styles: Styles = {
  heroBanner: {
    padding: '6rem 0',
    textAlign: 'center',
    position: 'relative',
    overflow: 'hidden',
  },
  buttons: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
};
