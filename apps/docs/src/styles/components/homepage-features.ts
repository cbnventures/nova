import type { Styles } from '@/types/index.d.ts';

/**
 * Styles.
 *
 * @since 1.0.0
 */
export const styles: Styles = {
  features: {
    display: 'flex',
    alignItems: 'center',
    padding: '2rem 0',
    width: '100%',
  },
};
