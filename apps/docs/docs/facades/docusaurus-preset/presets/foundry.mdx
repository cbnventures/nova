---
id: "foundry"
title: "Foundry"
description: "Warm orange and amber preset with Plus Jakarta Sans, Inter, and Fira Code fonts, rounded shapes, and elevated card depth."
sidebar_position: 2
keywords:
  - "nova"
  - "docusaurus facades"
  - "foundry preset"
  - "orange theme"
  - "plus jakarta sans"
  - "inter font"
  - "fira code"
  - "rounded shapes"
  - "elevated cards"
  - "bridge navbar"
  - "commons footer"
tags:
  - "facades"
  - "preset"
  - "foundry"
  - "docusaurus"
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

import ThemedImage from '@theme/ThemedImage';
import { Frame } from '@cbnventures/docusaurus-preset-nova/blocks';

import foundryLight from './_assets_/foundry-light.png';
import foundryDark from './_assets_/foundry-dark.png';

# Foundry

Warm orange and amber tones paired with Plus Jakarta Sans headings, Inter body text, and Fira Code for code blocks. Rounded shapes with comfortable spacing and elevated cards with soft shadows.

## Why Use This Preset?

- Warm orange palette so developer-facing documentation feels approachable rather than sterile.
- Plus Jakarta Sans headings paired with Inter body text so the typography balances personality with readability.
- Elevated cards and staggered reveals so content sections feel layered and polished.

## Preview

<Frame caption="Foundry preset — light and dark mode preview">
  <ThemedImage sources={{ light: foundryLight, dark: foundryDark }} alt="Foundry preset preview" width={1280} height={900} />
</Frame>

## Summary

Foundry is designed for developer tooling and infrastructure documentation. The warm palette communicates approachability while the elevated card depth adds visual hierarchy. Motion is set to normal speed with staggered reveals and hover effects enabled.

## Configuration

The full default configuration for the Foundry preset:

```ts
{
  preset: 'foundry',
  // Default values (no overrides needed):
  // colors.primary.light:    '#ea580c'
  // colors.primary.dark:     '#ea580c'
  // colors.secondary.light:  '#fbbf24'
  // colors.secondary.dark:   '#fbbf24'
  // colors.text.light:       '#1c1917'
  // colors.text.dark:        '#e7e5e4'
  // colors.border.light:     '#d6d3d1'
  // colors.border.dark:      '#57534e'
  // colors.warning.light:    '#f59e0b'
  // colors.warning.dark:     '#f59e0b'
  // colors.danger.light:     '#ef4444'
  // colors.danger.dark:      '#ef4444'
  // fonts.display:           'Plus Jakarta Sans'
  // fonts.body:              'Inter'
  // fonts.code:              'Fira Code'
  // navbar:                  'bridge'
  // footer:                  'commons'
}
```

## Overrides

Override any category to customize the Foundry identity. Unspecified values keep their defaults.

<Tabs groupId="foundry-overrides">
  <TabItem value="colors" label="Colors" default>

```ts title="docusaurus.config.ts"
{
  preset: 'foundry',
  overrides: {
    colors: {
      primary:   { light: '#ea580c', dark: '#ea580c' },
      secondary: { light: '#fbbf24', dark: '#fbbf24' },
      text:      { light: '#1c1917', dark: '#e7e5e4' },
      border:    { light: '#d6d3d1', dark: '#57534e' },
      warning:   { light: '#f59e0b', dark: '#f59e0b' },
      danger:    { light: '#ef4444', dark: '#ef4444' },
    },
  },
}
```

Each color category accepts a `{ light, dark }` pair. The `primary` and `secondary` colors expand to 11-level shade scales (50 through 950) as CSS custom properties.

  </TabItem>
  <TabItem value="fonts" label="Fonts">

```ts title="docusaurus.config.ts"
{
  preset: 'foundry',
  overrides: {
    fonts: {
      display: 'Plus Jakarta Sans',
      body: 'Inter',
      code: 'Fira Code',
    },
  },
}
```

Font families are set as CSS custom properties. Google Fonts are loaded automatically.

  </TabItem>
</Tabs>

## Navbar and Footer

Foundry uses the **Bridge** navbar and **Commons** footer.

| Slot   | Variant   | Description                                                     |
|--------|-----------|-----------------------------------------------------------------|
| Navbar | `bridge`  | Horizontal navigation bar with logo, links, and search.         |
| Footer | `commons` | Multi-column footer with sections, social links, and copyright. |

Override the variant in the preset options:

```ts title="docusaurus.config.ts"
{
  preset: 'foundry',
  overrides: {
    navbar: 'monolith',
    footer: 'ledger',
  },
}
```

Available navbar variants:

- `bridge`
- `canopy`
- `compass`
- `monolith`

Available footer variants:

- `commons`
- `embassy`
- `launchpad`
- `ledger`