import type { RuleTester } from '@typescript-eslint/rule-tester';

/**
 * Tests - Rules - ESLint - JSDoc - Require JSDoc Since.
 *
 * @since 0.15.0
 */
export type Tests_Rules_Eslint_Jsdoc_RequireJsdocSince_RuleTester = RuleTester;

/**
 * Tests - Rules - ESLint - JSDoc - Require JSDoc Since - Valid Filename.
 *
 * @since 0.20.0
 */
export type Tests_Rules_Eslint_Jsdoc_RequireJsdocSince_ValidFilename = string;

/**
 * Tests - Rules - ESLint - JSDoc - Require JSDoc Since - Zero Version Filename.
 *
 * Absolute path to a synthetic file inside the zero-version fixture directory,
 * used to verify that value validation is skipped when the nearest package.json
 * has version 0.0.0.
 *
 * @since 0.20.0
 */
export type Tests_Rules_Eslint_Jsdoc_RequireJsdocSince_ZeroVersionFilename = string;
