/**
 * Lib - Regex - Character Backslash.
 *
 * @since 0.18.0
 */
export type LibRegexCharacterBackslash = RegExp;

/**
 * Lib - Regex - Character Backtick.
 *
 * @since 0.18.0
 */
export type LibRegexCharacterBacktick = RegExp;

/**
 * Lib - Regex - Character Dollar.
 *
 * @since 0.18.0
 */
export type LibRegexCharacterDollar = RegExp;

/**
 * Lib - Regex - Character Double Quote.
 *
 * @since 0.11.0
 */
export type LibRegexCharacterDoubleQuote = RegExp;

/**
 * Lib - Regex - Character Pipe.
 *
 * @since 0.11.0
 */
export type LibRegexCharacterPipe = RegExp;

/**
 * Lib - Regex - Character Single Quote.
 *
 * @since 0.11.0
 */
export type LibRegexCharacterSingleQuote = RegExp;

/**
 * Lib - Regex - Linebreak CRLF Or LF.
 *
 * @since 0.11.0
 */
export type LibRegexLinebreakCrlfOrLf = RegExp;

/**
 * Lib - Regex - Pattern ANSI.
 *
 * @since 0.11.0
 */
export type LibRegexPatternAnsi = RegExp;

/**
 * Lib - Regex - Pattern ANSI Start.
 *
 * @since 0.11.0
 */
export type LibRegexPatternAnsiStart = RegExp;

/**
 * Lib - Regex - Pattern Camel Case Boundary.
 *
 * @since 0.11.0
 */
export type LibRegexPatternCamelCaseBoundary = RegExp;

/**
 * Lib - Regex - Pattern Camel Case Words.
 *
 * @since 0.11.0
 */
export type LibRegexPatternCamelCaseWords = RegExp;

/**
 * Lib - Regex - Pattern Casing Camel Case.
 *
 * @since 0.11.0
 */
export type LibRegexPatternCasingCamelCase = RegExp;

/**
 * Lib - Regex - Pattern Casing Pascal Case.
 *
 * @since 0.11.0
 */
export type LibRegexPatternCasingPascalCase = RegExp;

/**
 * Lib - Regex - Pattern Casing Upper Snake Case.
 *
 * @since 0.11.0
 */
export type LibRegexPatternCasingUpperSnakeCase = RegExp;

/**
 * Lib - Regex - Pattern Digits.
 *
 * @since 0.11.0
 */
export type LibRegexPatternDigits = RegExp;

/**
 * Lib - Regex - Pattern Double Quoted String Capture.
 *
 * @since 0.11.0
 */
export type LibRegexPatternDoubleQuotedStringCapture = RegExp;

/**
 * Lib - Regex - Pattern Email Simple.
 *
 * @since 0.11.0
 */
export type LibRegexPatternEmailSimple = RegExp;

/**
 * Lib - Regex - Pattern Env Line.
 *
 * @since 0.14.0
 */
export type LibRegexPatternEnvLine = RegExp;

/**
 * Lib - Regex - Pattern Env Var Key.
 *
 * @since 0.11.0
 */
export type LibRegexPatternEnvVarKey = RegExp;

/**
 * Lib - Regex - Pattern Env Var Key Screaming Snake.
 *
 * @since 0.11.0
 */
export type LibRegexPatternEnvVarKeyScreamingSnake = RegExp;

/**
 * Lib - Regex - Pattern Env Var Key Screaming Snake Inline.
 *
 * @since 0.11.0
 */
export type LibRegexPatternEnvVarKeyScreamingSnakeInline = RegExp;

/**
 * Lib - Regex - Pattern Error Prefix.
 *
 * @since 0.11.0
 */
export type LibRegexPatternErrorPrefix = RegExp;

/**
 * Lib - Regex - Pattern Example Value Inline.
 *
 * @since 0.11.0
 */
export type LibRegexPatternExampleValueInline = RegExp;

/**
 * Lib - Regex - Pattern Extension.
 *
 * @since 0.11.0
 */
export type LibRegexPatternExtension = RegExp;

/**
 * Lib - Regex - Pattern Extension Test Suffix.
 *
 * @since 0.15.0
 */
export type LibRegexPatternExtensionTestSuffix = RegExp;

/**
 * Lib - Regex - Pattern Gh Version.
 *
 * @since 0.22.0
 */
export type LibRegexPatternGhVersion = RegExp;

/**
 * Lib - Regex - Pattern Github Owner.
 *
 * @since 0.18.0
 */
export type LibRegexPatternGithubOwner = RegExp;

/**
 * Lib - Regex - Pattern Github Repo.
 *
 * @since 0.18.0
 */
export type LibRegexPatternGithubRepo = RegExp;

/**
 * Lib - Regex - Pattern Hash Border Line.
 *
 * @since 0.11.0
 */
export type LibRegexPatternHashBorderLine = RegExp;

/**
 * Lib - Regex - Pattern Java Version Line.
 *
 * @since 0.11.0
 */
export type LibRegexPatternJavaVersionLine = RegExp;

/**
 * Lib - Regex - Pattern JSDoc Line Prefix.
 *
 * @since 0.11.0
 */
export type LibRegexPatternJsdocLinePrefix = RegExp;

/**
 * Lib - Regex - Pattern Kebab Case Filename.
 *
 * @since 0.11.0
 */
export type LibRegexPatternKebabCaseFilename = RegExp;

/**
 * Lib - Regex - Pattern Leading Dot.
 *
 * @since 0.11.0
 */
export type LibRegexPatternLeadingDot = RegExp;

/**
 * Lib - Regex - Pattern Leading Dot Slash.
 *
 * @since 0.16.0
 */
export type LibRegexPatternLeadingDotSlash = RegExp;

/**
 * Lib - Regex - Pattern Leading Newlines.
 *
 * @since 0.11.0
 */
export type LibRegexPatternLeadingNewlines = RegExp;

/**
 * Lib - Regex - Pattern Leading Non Digits.
 *
 * @since 0.11.0
 */
export type LibRegexPatternLeadingNonDigits = RegExp;

/**
 * Lib - Regex - Pattern Leading Or Trailing Hyphen.
 *
 * @since 0.22.0
 */
export type LibRegexPatternLeadingOrTrailingHyphen = RegExp;

/**
 * Lib - Regex - Pattern Leading V.
 *
 * @since 0.11.0
 */
export type LibRegexPatternLeadingV = RegExp;

/**
 * Lib - Regex - Pattern Name At Version.
 *
 * @since 0.11.0
 */
export type LibRegexPatternNameAtVersion = RegExp;

/**
 * Lib - Regex - Pattern Non Topic Char.
 *
 * @since 0.22.0
 */
export type LibRegexPatternNonTopicChar = RegExp;

/**
 * Lib - Regex - Pattern Nova Prefix.
 *
 * @since 0.11.0
 */
export type LibRegexPatternNovaPrefix = RegExp;

/**
 * Lib - Regex - Pattern Range Capture Remainder.
 *
 * @since 0.11.0
 */
export type LibRegexPatternRangeCaptureRemainder = RegExp;

/**
 * Lib - Regex - Pattern Range Greater Equal Major.
 *
 * @since 0.11.0
 */
export type LibRegexPatternRangeGreaterEqualMajor = RegExp;

/**
 * Lib - Regex - Pattern Range Major.
 *
 * @since 0.11.0
 */
export type LibRegexPatternRangeMajor = RegExp;

/**
 * Lib - Regex - Pattern Rate Limit Reset.
 *
 * @since 0.22.0
 */
export type LibRegexPatternRateLimitReset = RegExp;

/**
 * Lib - Regex - Pattern Registry Query Line.
 *
 * @since 0.11.0
 */
export type LibRegexPatternRegistryQueryLine = RegExp;

/**
 * Lib - Regex - Pattern Rustc Version Line.
 *
 * @since 0.11.0
 */
export type LibRegexPatternRustcVersionLine = RegExp;

/**
 * Lib - Regex - Pattern Semver.
 *
 * @since 0.11.0
 */
export type LibRegexPatternSemver = RegExp;

/**
 * Lib - Regex - Pattern Semver Strict.
 *
 * @since 0.11.0
 */
export type LibRegexPatternSemverStrict = RegExp;

/**
 * Lib - Regex - Pattern Setup Bullet Line Capture.
 *
 * @since 0.11.0
 */
export type LibRegexPatternSetupBulletLineCapture = RegExp;

/**
 * Lib - Regex - Pattern Setup Header Line.
 *
 * @since 0.11.0
 */
export type LibRegexPatternSetupHeaderLine = RegExp;

/**
 * Lib - Regex - Pattern Setup Instructions Block.
 *
 * @since 0.11.0
 */
export type LibRegexPatternSetupInstructionsBlock = RegExp;

/**
 * Lib - Regex - Pattern Slug Scoped.
 *
 * @since 0.11.0
 */
export type LibRegexPatternSlugScoped = RegExp;

/**
 * Lib - Regex - Pattern Slug Simple.
 *
 * @since 0.11.0
 */
export type LibRegexPatternSlugSimple = RegExp;

/**
 * Lib - Regex - Pattern Topic Flag.
 *
 * @since 0.22.0
 */
export type LibRegexPatternTopicFlag = RegExp;

/**
 * Lib - Regex - Pattern Topic TypeScript.
 *
 * @since 0.22.0
 */
export type LibRegexPatternTopicTypescript = RegExp;

/**
 * Lib - Regex - Pattern Trailing Newlines.
 *
 * @since 0.16.0
 */
export type LibRegexPatternTrailingNewlines = RegExp;

/**
 * Lib - Regex - Pattern Trailing Newlines Or None.
 *
 * @since 0.16.0
 */
export type LibRegexPatternTrailingNewlinesOrNone = RegExp;

/**
 * Lib - Regex - Pattern Trailing Slash.
 *
 * @since 0.16.0
 */
export type LibRegexPatternTrailingSlash = RegExp;

/**
 * Lib - Regex - Pattern Whitespace.
 *
 * @since 0.11.0
 */
export type LibRegexPatternWhitespace = RegExp;

/**
 * Lib - Regex - Pattern Whitespace Or Underscore.
 *
 * @since 0.22.0
 */
export type LibRegexPatternWhitespaceOrUnderscore = RegExp;

/**
 * Lib - Regex - Pattern Workflow Context Expression.
 *
 * @since 0.21.0
 */
export type LibRegexPatternWorkflowContextExpression = RegExp;

/**
 * Lib - Regex - Pattern Workflow Context Separator.
 *
 * @since 0.21.0
 */
export type LibRegexPatternWorkflowContextSeparator = RegExp;

/**
 * Lib - Regex - Pattern Workflow Context Wrapper End.
 *
 * @since 0.21.0
 */
export type LibRegexPatternWorkflowContextWrapperEnd = RegExp;

/**
 * Lib - Regex - Pattern Workflow Context Wrapper Start.
 *
 * @since 0.21.0
 */
export type LibRegexPatternWorkflowContextWrapperStart = RegExp;

/**
 * Lib - Regex - Pattern Workflow Name.
 *
 * @since 0.21.0
 */
export type LibRegexPatternWorkflowName = RegExp;

/**
 * Lib - Regex - Pattern Workflow On Block.
 *
 * @since 0.21.0
 */
export type LibRegexPatternWorkflowOnBlock = RegExp;

/**
 * Lib - Regex - Pattern Workflow Placeholder.
 *
 * @since 0.21.0
 */
export type LibRegexPatternWorkflowPlaceholder = RegExp;

/**
 * Lib - Regex - Pattern Workflow Run Name Capture.
 *
 * @since 0.21.0
 */
export type LibRegexPatternWorkflowRunNameCapture = RegExp;

/**
 * Lib - Regex - Pattern Workflow Secret Reference.
 *
 * @since 0.20.0
 */
export type LibRegexPatternWorkflowSecretReference = RegExp;

/**
 * Lib - Regex - Pattern Workflow Var Reference.
 *
 * @since 0.20.0
 */
export type LibRegexPatternWorkflowVarReference = RegExp;

/**
 * Lib - Regex - Pattern Yml Extension.
 *
 * @since 0.21.0
 */
export type LibRegexPatternYmlExtension = RegExp;

/**
 * Lib - Regex - Placeholder Current Date.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderCurrentDate = RegExp;

/**
 * Lib - Regex - Placeholder Current Year.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderCurrentYear = RegExp;

/**
 * Lib - Regex - Placeholder Custom Donation.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderCustomDonation = RegExp;

/**
 * Lib - Regex - Placeholder Entity Name.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderEntityName = RegExp;

/**
 * Lib - Regex - Placeholder First Commit Year.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderFirstCommitYear = RegExp;

/**
 * Lib - Regex - Placeholder GitHub Org.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderGithubOrg = RegExp;

/**
 * Lib - Regex - Placeholder GitHub Repo.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderGithubRepo = RegExp;

/**
 * Lib - Regex - Placeholder GitHub Sponsor.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderGithubSponsor = RegExp;

/**
 * Lib - Regex - Placeholder GitHub URL.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderGithubUrl = RegExp;

/**
 * Lib - Regex - Placeholder Legal Agreements.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderLegalAgreements = RegExp;

/**
 * Lib - Regex - Placeholder Legal Label.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderLegalLabel = RegExp;

/**
 * Lib - Regex - Placeholder Legal Links.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderLegalLinks = RegExp;

/**
 * Lib - Regex - Placeholder npm Package.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderNpmPackage = RegExp;

/**
 * Lib - Regex - Placeholder Our.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderOur = RegExp;

/**
 * Lib - Regex - Placeholder Platform Badges.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderPlatformBadges = RegExp;

/**
 * Lib - Regex - Placeholder Platform Fields.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderPlatformFields = RegExp;

/**
 * Lib - Regex - Placeholder Privacy Policy.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderPrivacyPolicy = RegExp;

/**
 * Lib - Regex - Placeholder Project Description.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderProjectDescription = RegExp;

/**
 * Lib - Regex - Placeholder Project Homepage.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderProjectHomepage = RegExp;

/**
 * Lib - Regex - Placeholder Project Link.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderProjectLink = RegExp;

/**
 * Lib - Regex - Placeholder Project Logo URL.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderProjectLogoUrl = RegExp;

/**
 * Lib - Regex - Placeholder Project Name.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderProjectName = RegExp;

/**
 * Lib - Regex - Placeholder Project Slug.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderProjectSlug = RegExp;

/**
 * Lib - Regex - Placeholder Terms Of Use.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderTermsOfUse = RegExp;

/**
 * Lib - Regex - Placeholder Us.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderUs = RegExp;

/**
 * Lib - Regex - Placeholder We.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderWe = RegExp;

/**
 * Lib - Regex - Placeholder Year Range.
 *
 * @since 0.11.0
 */
export type LibRegexPlaceholderYearRange = RegExp;

/**
 * Lib - Regex - URL Prefix Buy Me A Coffee.
 *
 * @since 0.11.0
 */
export type LibRegexUrlPrefixBuyMeACoffee = RegExp;

/**
 * Lib - Regex - URL Prefix Docker Hub.
 *
 * @since 0.11.0
 */
export type LibRegexUrlPrefixDockerHub = RegExp;

/**
 * Lib - Regex - URL Prefix GitHub Sponsors.
 *
 * @since 0.11.0
 */
export type LibRegexUrlPrefixGithubSponsors = RegExp;

/**
 * Lib - Regex - URL Prefix Ko-fi.
 *
 * @since 0.11.0
 */
export type LibRegexUrlPrefixKoFi = RegExp;

/**
 * Lib - Regex - URL Prefix Liberapay.
 *
 * @since 0.11.0
 */
export type LibRegexUrlPrefixLiberapay = RegExp;

/**
 * Lib - Regex - URL Prefix npm Package.
 *
 * @since 0.11.0
 */
export type LibRegexUrlPrefixNpmPackage = RegExp;

/**
 * Lib - Regex - URL Prefix Open Collective.
 *
 * @since 0.11.0
 */
export type LibRegexUrlPrefixOpenCollective = RegExp;

/**
 * Lib - Regex - URL Prefix Patreon.
 *
 * @since 0.11.0
 */
export type LibRegexUrlPrefixPatreon = RegExp;

/**
 * Lib - Regex - URL Prefix PayPal.
 *
 * @since 0.11.0
 */
export type LibRegexUrlPrefixPaypal = RegExp;
