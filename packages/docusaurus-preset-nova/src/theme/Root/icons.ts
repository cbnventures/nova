/**
 * Theme - Root - Icons.
 *
 * Registers the build-time, tree-shaken icon set the preset's icon plugin
 * generates from the site's content and theme configuration, so every
 * referenced icon resolves offline without bundling whole Iconify collections.
 *
 * @since 0.15.0
 */
import '@nova-generated-icons';
