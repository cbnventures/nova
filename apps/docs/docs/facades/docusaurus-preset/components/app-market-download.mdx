---
id: app-market-download
title: App Market Download
description: App store download buttons for Apple App Store and Google Play with preset-aware styling.
sidebar_position: 7
keywords:
  - nova
  - docusaurus facades
  - app market download
  - app store button
  - google play button
  - download links
tags:
  - facades
  - component
  - app market download
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# App Market Download

App store download buttons for Apple App Store and Google Play. Renders styled buttons that open in a new tab.

Returns nothing if both URLs are omitted.

## Summary

The App Market Download component renders a `<section>` with one or two download buttons. Each button links to the respective app store. The component renders nothing if neither `appStoreUrl` nor `googlePlayUrl` is provided.

## Why Use This Component?

- **Consistent download buttons** that match the active preset styling.
- **Conditional rendering** — only shows buttons for stores where a URL is provided.
- **Accessible** — links open in a new tab with proper `rel="noopener noreferrer"` attributes.

## Usage

<Tabs groupId="app-market-download-usage">
  <TabItem value="both" label="Both stores" default>

```tsx title="src/pages/index.tsx"
import { AppMarketDownload } from '@cbnventures/docusaurus-preset-nova/components';

<AppMarketDownload
  label="Download the app"
  appStoreUrl="https://apps.apple.com/app/example"
  googlePlayUrl="https://play.google.com/store/apps/details?id=example"
/>
```

  </TabItem>
  <TabItem value="single" label="Single store">

```tsx title="src/pages/index.tsx"
import { AppMarketDownload } from '@cbnventures/docusaurus-preset-nova/components';

<AppMarketDownload
  appStoreUrl="https://apps.apple.com/app/example"
  surface="alt"
/>
```

  </TabItem>
</Tabs>

## Options

| Prop            | Type                  | Default     | Description                                             |
|-----------------|-----------------------|-------------|---------------------------------------------------------|
| `appStoreUrl`   | `string \| undefined` | `undefined` | Apple App Store URL. Button hidden if not set.          |
| `googlePlayUrl` | `string \| undefined` | `undefined` | Google Play Store URL. Button hidden if not set.        |
| `label`         | `string \| undefined` | `undefined` | Optional label text above the buttons.                  |
| `surface`       | `'alt' \| undefined`  | `undefined` | Set to `'alt'` to use the alternate surface background. |

## Troubleshooting

- **Nothing rendering** — The component returns nothing if both `appStoreUrl` and `googlePlayUrl` are `undefined`. Set at least one URL.
