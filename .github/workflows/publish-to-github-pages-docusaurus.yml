name: "Publish to GitHub Pages (Docusaurus)"

run-name: "Publishing ${{ github.event.release.tag_name || 'manually' }} to GitHub Pages"

on:
  release:
    types: [ "published" ]
  workflow_dispatch:

permissions:
  contents: "read"
  id-token: "write"
  pages: "write"

jobs:
  publish-to-github-pages:
    runs-on: "ubuntu-latest"
    timeout-minutes: 10
    env:
      ROOT_DIR: "${{ vars.ROOT_WORKING_DIR }}"
      WORKING_DIR: "${{ vars.DOCUSAURUS_WORKING_DIR }}"
    steps:
      - name: "Checkout repository"
        uses: "actions/checkout@v6"

      - name: "Setup Node.js"
        uses: "actions/setup-node@v6"
        with:
          node-version-file: "${{ env.ROOT_DIR }}/package.json"
          package-manager-cache: false

      - name: "Update npm"
        run: "npm install -g npm@latest"
        working-directory: "${{ env.ROOT_DIR }}"

      - name: "Install packages"
        run: "npm install"
        working-directory: "${{ env.ROOT_DIR }}"

      - name: "Build project"
        run: "npm run build"
        working-directory: "${{ env.ROOT_DIR }}"

      - name: "Setup GitHub Pages"
        uses: "actions/configure-pages@v5"

      - name: "Upload artifact to GitHub Pages"
        uses: "actions/upload-pages-artifact@v4"
        with:
          path: "${{ env.WORKING_DIR }}/build"

      - name: "Deploy to GitHub Pages"
        uses: "actions/deploy-pages@v4"
