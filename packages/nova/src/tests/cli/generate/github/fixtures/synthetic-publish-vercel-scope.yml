# This file is generated by @cbnventures/nova.
# Do not edit manually.
#
# Run `nova generate github workflows` to regenerate.
# See: https://nova.cbnventures.io/docs/cli/generators/github/workflows

name: "Publish (vercel-scope)"

run-name: "Publishing ${{ github.event.release.tag_name || 'manually' }} (vercel-scope)${{ github.event_name == 'workflow_dispatch' && inputs.dry-run && ' (dry run)' || '' }}"

on:
  release:
    types:
      - "published"
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Run without making changes"
        required: false
        type: "boolean"
        default: true

permissions:
  contents: "read"

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: false

env:
  PUBLISH: "${{ github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && !inputs.dry-run) }}"
  ROOT_DIR: "./"

jobs:
  build:
    runs-on: "ubuntu-latest"
    timeout-minutes: 15
    permissions:
      contents: "read"
    steps:
      - name: "Checkout repository"
        uses: "actions/checkout@v7"
        with:
          fetch-depth: 0

      - name: "Setup Node.js"
        uses: "actions/setup-node@v6"
        with:
          node-version-file: "${{ env.ROOT_DIR }}/package.json"
          package-manager-cache: false

      - name: "Install packages"
        run: |
          npm install -g npm@latest
          npm install
        working-directory: "${{ env.ROOT_DIR }}"

      - name: "Check project"
        run: |
          npx turbo run check --filter=nova-app-web --concurrency=2
        working-directory: "${{ env.ROOT_DIR }}"

      - name: "Build project"
        run: |
          npx turbo run build --filter=nova-app-web --concurrency=2
        working-directory: "${{ env.ROOT_DIR }}"

      - name: "Upload build artifacts (vercel-nextjs/apps-web)"
        uses: "actions/upload-artifact@v7"
        with:
          name: "build-vercel-nextjs-apps-web"
          retention-days: 1
          path: |
            apps/web/build
            apps/web/public

  publish-vercel-nextjs-apps-web:
    needs: "build"
    runs-on: "ubuntu-latest"
    timeout-minutes: 15
    permissions:
      contents: "read"
    steps:
      - name: "Checkout repository"
        uses: "actions/checkout@v7"

      - name: "Setup Node.js"
        uses: "actions/setup-node@v6"
        with:
          node-version-file: "${{ env.ROOT_DIR }}/package.json"
          package-manager-cache: false

      - name: "Download build artifacts"
        uses: "actions/download-artifact@v8"
        with:
          name: "build-vercel-nextjs-apps-web"

      - name: "Install Vercel CLI"
        if: "${{ env.PUBLISH == 'true' }}"
        run: npm install -g vercel@latest

      - name: "Pull Vercel environment"
        if: "${{ env.PUBLISH == 'true' }}"
        env:
          VERCEL_TOKEN: "${{ secrets.PF_VERCEL_TOKEN }}"
          VERCEL_ORG_ID: "${{ vars.PF_VERCEL_ORG_ID }}"
          VERCEL_PROJECT_ID: "${{ vars.WEB_VERCEL_PROJECT_ID }}"
        run: |
          vercel pull --yes --environment=production --token="${VERCEL_TOKEN}"
        working-directory: "./apps/web"

      - name: "Deploy to Vercel"
        if: "${{ env.PUBLISH == 'true' }}"
        env:
          VERCEL_TOKEN: "${{ secrets.PF_VERCEL_TOKEN }}"
          VERCEL_ORG_ID: "${{ vars.PF_VERCEL_ORG_ID }}"
          VERCEL_PROJECT_ID: "${{ vars.WEB_VERCEL_PROJECT_ID }}"
        run: |
          vercel deploy --prebuilt --prod --token="${VERCEL_TOKEN}"
        working-directory: "./apps/web"
