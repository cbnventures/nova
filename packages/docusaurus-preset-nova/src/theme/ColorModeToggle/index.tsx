/**
 * Theme - Color Mode Toggle - Color Mode Toggle.
 *
 * Intentionally renders nothing because the bare base preset handles color
 * mode switching directly inside the Navbar component, avoiding broken
 * rendering after the CSS revert layer strips framework styles.
 *
 * @constructor
 *
 * @since 0.15.0
 */
function ColorModeToggle() {
  return undefined;
}

export default ColorModeToggle;
